export interface IAddressArea {
    id: number
    description: string
}

export interface IAddressSettlement {
    id: number
    description: string
}

export interface IAddressWarehouse {
    id: number,
    description: string,
    longitude: number,
    latitude: number,
}